<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link href="/css/style.css" type="text/css" rel="stylesheet">
</head>

<body>

    <div id="container">
        <div>
            {{#if loading}}
            <div class="notification loadingBox">
                Loading …
            </div>
            {{/if}}
            {{#if info}}
            <div class="notification infoBox">
                <span>{{info}}</span>
            </div>
            {{/if}}
            {{#if error}}
            <div class="notification errorBox">
                <span>{{error.message}}</span>
            </div>
            {{/if}}
        </div>

        <nav>
            <div class="left-container">
                <ul>
                    <li><a href="/">Home</a></li>
                    {{#if isAuthenticated}}
                    <li><a href="/products/add-hotel">Add +</a></li>
            </div>
            <div class="right-container">
                    <li><a href="/auth/profile">{{user.username}}</a></li>

                    <li class="log-out"><a href="/auth/logout">Logout</a></li>
                    {{else}}
                    <li><a href="/auth/login">Login</a></li>
                    <li><a href="/auth/register">Register</a></li>
                    {{/if}}
                </ul>
            </div>
            
        </nav>
        {{{body}}}
<footer>@SoftUni - JS Back-end - BookingUni 2020</footer>
    </div>

</body>

</html>